# 核心依赖（必需）
requests==2.32.5          # HTTP 请求：调用 OpenRouter LLM API 及 OCR 服务
pillow==9.5.0             # 图像读写（PIL.Image），图像压缩与格式转换
opencv-python==4.11.0.86  # 图像预处理（去噪、对比度增强，仅用于 OCR 输入）
numpy==1.26.4             # 数值计算，配合 OpenCV 使用
pymupdf==1.27.1           # PDF → PNG 转换（600 DPI），import fitz

# OCR 服务依赖（可选，--no-ocr 时不需要）
# 启动方式：cd ocr_service && python ocr_server.py
flask==3.1.3              # OCR HTTP 服务（端口 5000）
paddlepaddle==3.2.0       # PaddleOCR 运行时（严格固定：3.3.0 存在 oneDNN/PIR bug 导致所有 OCR 请求返回 500）
paddleocr==3.4.0          # OCR 识别引擎

# 3D 重建依赖（可选，仅 scripts/json_to_vtu.py 使用）
pyvista==0.47.1           # JSON 结构数据 → VTU 三维模型输出
